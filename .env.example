# =============================================================================
# DevHub Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in the values
# NEVER commit .env.local to version control!

# =============================================================================
# DATABASE (Vercel Postgres / Neon)
# =============================================================================
# Get these from Vercel Dashboard → Storage → [Database] → .env.local tab
# Or run: vercel env pull .env.local

# Primary connection string (pooled) - used for all database queries
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# Direct connection string (unpooled) - used for Prisma migrations
# Same as DATABASE_URL but remove "-pooler" from the hostname
DATABASE_URL_UNPOOLED="postgresql://user:password@host:5432/database?sslmode=require"

# =============================================================================
# PAYLOAD CMS
# =============================================================================
# Secret for Payload CMS session encryption (generate a random string)
PAYLOAD_SECRET="your-payload-secret-here"

# =============================================================================
# CLERK AUTHENTICATION
# =============================================================================
# Get these from Clerk Dashboard → API Keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_..."
CLERK_SECRET_KEY="sk_test_..."

# Webhook secret for Clerk webhooks (from Clerk Dashboard → Webhooks → Signing Secret)
CLERK_WEBHOOK_SECRET="whsec_..."

# Clerk redirect URLs (optional - can also be set in Clerk dashboard)
NEXT_PUBLIC_CLERK_SIGN_IN_URL="/sign-in"
NEXT_PUBLIC_CLERK_SIGN_UP_URL="/sign-up"
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL="/home"
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL="/home"

# =============================================================================
# VERCEL (Auto-populated when deployed)
# =============================================================================
# These are automatically set by Vercel during deployment
# VERCEL_URL="..."
# VERCEL_ENV="development|preview|production"
